# aio-project-1

> 공개 범위: **비공개(Private)**

## 실행 방법
```
python -m venv .venv          # 최초 1회
venv                          # 가상환경 켜기
pip install -r requirements.txt
```

## 환경변수 설정
```
copy .env.example .env
```
그 뒤 `.env` 를 열어 실제 API 키를 채웁니다. (`.env` 는 GitHub에 올라가지 않습니다)

## 팀원
- 신영석
